// src/components/Header.tsx
import React from "react";

const Header: React.FC = () => {
  return (
    <header className="flex justify-between text-indigo-800 items-center mb-8">
    </header>
  );
};

export default Header;
